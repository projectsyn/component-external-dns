parameters:
  kapitan:
    compile:
      - input_paths:
          - external-dns/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - external-dns/component/main.jsonnet
        input_type: jsonnet
        output_path: external-dns/
